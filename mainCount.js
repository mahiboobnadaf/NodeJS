// const counting = require("./count");

const stuff = require("./count")

console.log(stuff.counter(['abc','def','ghi','jkl']));

console.log(stuff.adder(20,20));

console.log(stuff.adder(stuff.pi,30));
